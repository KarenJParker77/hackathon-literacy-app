import { initialState } from "./initialState";
// import types

export function reducer(state = initialState, action) {
  switch (action.type) {
    case 1:
      break;

    case 2:
      break;

    default:
      return state;
  }
}
